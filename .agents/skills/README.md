# Skills Locales

Main no mantiene skills lifecycle ni defaults de runtime. OMP descubre
capacidades locales reales cuando hacen falta, sin que esta carpeta prescriba
su selección o ejecución.

`docs/skills/` es el canon de Main. `.agents/skills` permanece como junction
estable para discovery del harness hacia esta carpeta.
