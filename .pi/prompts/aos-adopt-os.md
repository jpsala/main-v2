---
description: Redirigir adopciones AOS al upstream manager
---
Este repo ya es un downstream AOS adoptado. Si JP pide `aos-adopt-os` para otro repo, no lo ejecutes desde aca ni copies manager-only: indica usar `C:/dev/os` como upstream manager. Si el objetivo real es revisar este repo, ejecuta `aos-realinear-os`/`aos-perfect-os` sobre la capa agentica local, sin tocar runtime, datos ni config.
