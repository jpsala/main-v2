# Tracks

Trabajos vivos retomables. No guardar transcript; guardar estado, next step y refs.

## Reglas

- Cada track tiene frontmatter con `status`, `updated`, `priority`.
- Archivar cuando cierre o cuando lo durable se haya promovido a docs estables.
- Mantener referencias a archivos existentes.
